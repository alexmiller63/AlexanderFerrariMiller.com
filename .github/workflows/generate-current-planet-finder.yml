name: Generate Planet Finder - RETIRED

on:
  workflow_dispatch:

env:
  STAR_ALMANACK_EPHEMERIS_DIR: ${{ github.workspace }}/.cache/skyfield

permissions:
  contents: write

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RETIRED: use Generate Current Planet Finders, which calls tools/generate_planet_finders.py"
          exit 1
