name: Populate Sky Notes — by ISO date

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: Start date (ISO YYYY-MM-DD)
        required: true
        type: string
      end_date:
        description: End date (ISO YYYY-MM-DD)
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: populate-sky-notes-by-date
  cancel-in-progress: false

jobs:
  populate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Create and populate Sky Notes prose for requested ISO dates
        run: python tools/populate_sky_notes_by_date.py "${{ inputs.start_date }}" "${{ inputs.end_date }}"

      - name: Commit Sky Notes prose and generated sources only
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- almanack Star-Almanack-Repo/site
          git add --all -- Star-Almanack-Repo/sky-notes-*
          if git diff --cached --quiet; then
            echo "No Sky Notes prose changes in requested ISO date range."
            exit 0
          fi
          git commit -m "Populate Sky Notes ${{ inputs.start_date }} through ${{ inputs.end_date }}"
          git pull --rebase origin main
          git push
